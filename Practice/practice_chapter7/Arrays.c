#include <stdio.h>

int main(){
    int marks[90];

    marks[0] = 45;
    marks[1] = 95;
// we can go till Marks[89]
    printf("The Marks 0 and Marks 1 is %d and %d", marks[0], marks[1]);
    return 0;
}